---
import Layout from '../layouts/layout.astro';

const links = [
    { name: "LinkedIn", url: "https://www.linkedin.com/in/alessio-spineto-8895b9251/", icon: "👔" },
    { name: "GitHub", url: "https://github.com/Comodaino", icon: "💻" },
    { name: "Email", url: "mailto:alespineto@email.com", icon: "✉️" }
];
---

<Layout title="Links">
    <div class="max-w-md mx-auto py-10">
        <h2 class="text-3xl font-bold mb-8 text-center">Links</h2>

        <div class="flex flex-col gap-4">
            {links.map((link) => (
                    <a href={link.url} target="_blank" class="flex items-center p-4 bg-white rounded-xl shadow border border-slate-100 hover:scale-105 hover:shadow-lg transition transform duration-200 dark:bg-slate-800 dark:border-slate-700 dark:text-white dark:hover:bg-slate-700">
                        <span class="text-2xl mr-4">{link.icon}</span>
                        <span class="font-semibold text-lg">{link.name}</span>
                    </a>
            ))}
        </div>
    </div>
</Layout>